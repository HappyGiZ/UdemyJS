'use strict';


const sliderFunctions = {
    plus(a, b) {
        return a + b;
    },
    minus(a, b) {
        return a - b;
    }
};

console.log(sliderFunctions.plus(3, 4) * sliderFunctions.minus(5, 1));

